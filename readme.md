# Readme.md

edit the file: `mapping.py` and add additional attacks, to test the queries use the fast api frontend. Just enter the attack id and it will tell you if the query was successful.

to run the fast api frontend, run the following command:

```bash
uvicorn main:app --reload
```
